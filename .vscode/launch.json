{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "My simple debug", // can be any name, appears in the debug dropdown
			"request": "launch",
			"type": "cppdbg",
			"program": "${workspaceRoot}/a.out",
			"cwd": "${workspaceFolder}",
			"MIMode": "lldb"
		},
	]
}